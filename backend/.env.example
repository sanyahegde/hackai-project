# MongoDB connection string (e.g. MongoDB Atlas)
# Get one at https://cloud.mongodb.com/ — free tier available
MONGODB_URI=

# Database name (default: learnflow)
MONGODB_DB_NAME=learnlens

# YouTube Data API key
# Get one at https://console.cloud.google.com/ → APIs & Services → Credentials
YOUTUBE_API_KEY=
